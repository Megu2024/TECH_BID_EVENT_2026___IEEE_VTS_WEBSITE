{
  "name": "tech-bid-event-2026",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "jsonwebtoken": "^9.0.3"
  }
}
